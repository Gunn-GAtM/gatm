% I MADE SOME TENTATIVE CHANGES, WIP, WILL BE BETTER LATER

\documentclass[11pt, a4paper]{article}
% 11 point font, A4-sized paper

\usepackage[utf8]{inputenc}
% Specifies that the input is UTF-8
% thanks for specifying that, i was unsure what "utf8" meant lmao

\usepackage[inline]{asymptote}

\usepackage{anyfontsize}

\begin{document}

\begin{titlepage}
	% Title page
    \centering  		% keeps everything centered
    \vfill 				% Fills up the top with whitespace
    
    {
    
        { \fontsize{30}{30} \selectfont	% Boldface, large font
        	A Geometric Approach to } % please intent properly lol
        \par \vspace{.5cm}
        { \fontsize{60}{60} \selectfont	% Massive font
        	Matrices } % i changed some of the styling for flow & shape
        
        \vspace{3cm}		% Some vertical space
        
        % Cover page figure
        
       	\begin{asy} // I HATE THIS SO MUCH EWW
			size(200);
			
			pair cis(real theta) { // Gives points on the unit circle
				return (cos(theta), sin(theta));
			}
			
       		pair A = cis(-pi/6), B = cis(pi/2), C = cis(7*pi/6); // Starting triangle corners
		    
		    for (int i = 0; i < 4; ++i) { // Draws the triangles and dots
			    draw(A--B--C--cycle);
			    
			    dot(A);
			    dot(B);
			    dot(C);
			    
		    	if (i < 3) {
					A *= -2; B *= -2; C *= -2; // Scales A,B,C to the next triangle
				}
		    }
		    
		    dot((0,0)); // Center dot
		    
		    draw(A--(-1/2 * A), dashed);
		    draw(B--(-1/2 * B), dashed);
		    draw(C--(-1/2 * C), dashed);
		    
		    
		    draw(circle((0,0), 8));
		    
       	\end{asy}
	
        \vspace{3cm}
        
		{ \fontsize{30}{30} \selectfont by Peter Herreshoff \textit{et al.} }
		\par % QUESTION: why et al.? are you planning on writing new stuff?
		
        \vspace{0.25cm}
        
		{ \fontsize{18}{18} \selectfont Gunn High School Analysis H }
		
		\vfill
    }
    
\end{titlepage}

\end{document}